# Dashboard — chat seed

Paste at the start of a fresh planning chat to resume. (I also carry memory of this project across chats, but this guarantees the specifics.)

## What this is
A personal productivity dashboard. It **began as a learning vehicle** for the Claude Code harness (CLAUDE.md / rules / skills / hooks / subagents, runtime agent pipelines) — **from g6 the objective flipped: prod-usability for the owner's daily personal use is primary**, harness learning is secondary (friction-driven `.claude/` upkeep via each goal's closing checklist; no capability introduced for its own sake). Goals 6–7 (+ the 7a/7b/7c polish set) are the personal-MVP set. **Ladder culled 2026-07-07:** the MVP settled in daily use, so **g8 = deploy-ready** (multi-user Google Sign-In + EC2 — wife and close friends join) was written and **shipped**; **g8a** hardened the notes target across releases and **g9 = user-defined notes hierarchy + hierarchical routing** shipped on top. Granola is now **g10**; the old g9/g10/g11 (layout-prefs∥calendar, plugin capstone, dependency views) are dropped. **As of 2026-07-12 the app is deployed and in multi-user daily use through g9.**

## Workflow
- Strategy + "why": the Claude.ai planning chat (has cross-chat memory).
- Execution: Claude Code in VS Code, one fresh session per goal.
- Handoff: a terse, agent-facing brief committed to `docs/goals/goal-N.md`; Claude Code reads it and executes.
- One fresh planning chat per goal, seeded by this doc.

## Stack & repo
- Monorepo: FastAPI (Python) + React (Vite + TS). SQLite local / Postgres prod, used only for the task-metadata overlay (rank, grouping) and small UI-state persistence later (`ui_prefs`, layout goal).
- Persistence: **SQLModel + Alembic** (introduced g2; g3 added `task_group` + dropped priority). Reused for the scratchpad store and review queue (g5) and `ui_prefs` (g7).
- Frontend DnD: **@dnd-kit**, one `DndContext` per list, flat SortableContext per bucket, each bucket a `useDroppable`. All DnD architecture, bug history, and rank rules live in `.claude/rules/tasks-panel.md` (path-scoped) — the seed doesn't duplicate them.
- Repo: github.com/kartpop/dashboard

## Key decisions
- **Single identity:** everything runs on a personal @gmail account. Dropped the projecttech4dev org dependency (not the t4d Workspace admin, so org policy would gate access). One OAuth client, one token.
- t4d calendar visibility (share into personal account) is a parked, optional experiment — not a dependency.
- Calendar is read-only in the app for v1; invites added manually in Google.
- **Ladder reordered after g3 (learning-first):** Google writes were pulled forward to g4 (was g6) because the thin read-only polish goals (layout, calendar) are independent and low-risk — the right material for a later parallel-worktrees rep, not sequential solo goals. **g4a inserted after g4** to complete the write/CRUD surface — also a g5 prerequisite (the router creates tasks). The capture→route→Granola pipeline follows the write machinery it depends on. **From g4a the tasks surface graduates to a daily-driver MVP** (real personal use against real Google lists); Calendar / Scratchpad / router stay WIP. *(Supersedes the earlier "daily-driver deferred" stance — for the tasks surface only.)*
- **Priority was dropped in g3** (unrequested g2 feature; position within a bucket is the importance signal).
- **Grouping is overlay-only** (shipped g3): users cluster tasks into named groups inside a `(list, date-bucket)`; Google still sees a flat list per list. Overlay rank + grouping override Google's order at render time. Group deletion ungroups members via FK `ON DELETE SET NULL`.
- **Groups never span buckets** (g4): a cross-bucket drag updates only the dragged task's row — `group_id` = the destination group if dropped into one, else `NULL`; source-group siblings untouched.
- **Google write surface:** g4 = task **metadata** (due date, list membership). g4a = task **content** (create, edit title/notes, **complete/uncomplete**, delete) + list rename. **Rank/grouping never sync to Google** — overlay-only. Cross-list move = insert-then-delete (the API has no between-lists move) → new task ID → overlay row migrates.
- **No confirm dialogs on writes** (deliberate reversal of the original g6 plan). Reschedule's undo is drag-back. The one genuinely irreversible op — **delete (g4a)** — gets a **deferred-delete + undo-toast**: optimistic remove, ~5s "Undo" toast, the Google `DELETE` fires only after the window closes (Undo cancels with zero Google writes). **Completion** uses an undo-toast too but writes the Google status change **immediately** (Google retains completed tasks; uncomplete is cheap) — the toast is mis-click recovery and the "it vanished" affordance. Every other write is optimistic with a pre-op snapshot; failure → rollback + error toast (the one place errors are NOT swallowed).
- **New tasks land in `NO_DATE`, top of bucket** (g4a) — matches Google's add-task default; date set via the picker (below) or drag.
- **Arbitrary due dates via a date-picker** (g4a): a date control on each task and on create calls the existing g4 `reschedule` endpoint, so dates *outside* the rendered buckets are reachable — drag-reschedule only reaches buckets that already exist. **Overdue** tasks roll up into an Overdue bucket at the top (not scattered in past-date buckets). **Manual refresh** per panel (re-run `GET /tasks`) handles staleness vs. the phone app and surfaces a recurring task's next instance after completion. **Subtasks** render flat as standalone for MVP (a `parent` task is never dropped/duplicated); hierarchy UI deferred unless the user relies on it.
- **View prefs (layout goal) persist server-side** in a `ui_prefs` key-value table, not `localStorage`. *(Deferred past the MVP set — g6 pins lists statically in code; `ui_prefs` + visibility chooser move to g9a.)*
- **Objective shift after g5 (MVP reorder):** prod daily use is now primary. g6 = MVP layout (`My Tasks | Follow-ups | Scratchpad` full-width; cross-list drag pulled forward from old-g7a; shared `DndContext` across the pinned pair; `move` + optional `due_date`/`group_id` — shipped); g7 = scratchpad v2 (bullet editor + `note` → Google Doc). Granola/layout-prefs∥calendar/plugin renumbered to g8/g9/g10.
- **g7 locks (user-confirmed 2026-07-06):** Capture = **whole editor as ONE entry** (no bullet-splitting); editor **clears on capture**; Doc notes **insert at top** (newest-first) under an H3 timestamp (`6-July-2026, 8:41 PM IST`); doc target = `NOTES_DOC_ID` env var (unset → kept-local fallback); verbatim plain text (no Docs-native bullets); plain textarea, no rich-text lib. Router write set grows to `{create_task, reschedule, append_note}` — insert-only, all guardrail artifacts (router.md / writes.md / AST test / eval gate) revised in lockstep.
- **g7c locks (2026-07-07):** routing goes **synchronous in `POST /scratch`** (capture persists first, then routes inline; response carries the routed state; failure → `UNROUTED` + 2xx; scheduler → ~15-min retry backstop); the g7 verbatim lock is **deliberately amended** — the Doc entry gains one LLM-authored line (a summary one-liner between timestamp and verbatim raw text; write set + insert-only unchanged; `router.md`/`writes.md`/evals revised in lockstep, summary not eval-graded); review queue = **task | note only** (`event` still a classifier output → review, no longer a resolution type) with in-place editable fields per type; add-task row gains the date picker.
- **g8 locks (2026-07-07, deploy):** consent screen **published unverified** (testing mode's 7-day refresh-token expiry is the disqualifier; ≤100-user cap irrelevant); notes folder + Doc **app-created** in each user's Drive (`drive.file` cannot write into user-chosen folders — no Picker; `NOTES_FOLDER_ID`/`NOTES_DOC_ID` die), calendar selection = **toggle list** from `calendarList.list` (replaces `EXTRA_CALENDAR_IDS`) — env vars → per-user `user_settings`; hosting = **one small EC2 (~$10/mo) + Cloudflare DNS + Caddy auto-TLS, single container** (FastAPI serves the Vite build); **SQLite stays** (WAL + volume + backup cron; Postgres only when it hurts — it and the in-process scheduler share the single-instance assumption); security floor = `ALLOWED_EMAILS` allowlist + signed HttpOnly/Secure/Lax cookie session + one `current_user` dependency scoping every query + Fernet-encrypted refresh tokens + per-token scope assertion; pinned lists stay **convention-by-title** ("My Tasks" is Google's default; "Follow-ups" created once); one app-level `ANTHROPIC_API_KEY` (owner pays for everyone's Haiku routing).
- **Drive/Docs scoping (locked 2026-07-07; ADR `docs/goals/architecture/drive-access-scoping.md`):** folder-level OAuth consent doesn't exist in Google's model → Option A: OAuth scope is **`drive.file` only** (Google-enforced: the app can only touch files it created; never `documents`/`drive`); a bootstrap command creates the notes Doc inside `NOTES_FOLDER_ID`; folder-ancestry gate + startup scope assertion + AST-test extension enforce the boundary in code; doc/folder IDs are config-only — never LLM output. One-time re-auth + revoke of the old broad grant; g7 ships an **owner-steps checklist** (`goal-7-owner-steps.md`) for all non-code actions — a process rule for every future scope/setup change. Reading hand-made files under the folder deferred to g8 (service account = leading candidate; hybrid `drive.readonly` the fallback).

## Harness facts established
- Claude Code reads **CLAUDE.md**, not AGENTS.md (skip AGENTS.md while solo). Loads in full every session → keep lean (<120 lines).
- Three instruction tiers: **CLAUDE.md** (always loaded, stable facts) / **`.claude/rules/`** (path-scoped via `paths:` frontmatter; loads only when touching matching files) / **skills** (`.claude/skills/<name>/SKILL.md`, on-demand).
- **Path-scoped rules are flaky** (load globally, never load, or load on Read but not Write); verify with `/context`. g4 finding: in a **team run**, rule firing is **per-teammate-context and invisible to the lead** — `backend.md` fired in the lead's session, but `tasks-panel.md` / `writes.md` were edited inside teammates' contexts so their probe was inconclusive from the lead's seat. **To probe a rule, have the teammate that touches its files echo `/context`** (carried into g4a). **g4a reading (inline analog — no teammates that run):** in a *single* context all four path-scoped rules fired — `backend.md` + `writes.md` on backend edits, `frontend.md` + `tasks-panel.md` on the tasks-panel edits. Single-context scoping works; the **per-teammate-firing** question is still untested → rolls to g7a∥b.
- Agent-facing docs (CLAUDE.md, rules, goal briefs) = terse, imperative, checkable, only error-preventing rationale. The "why" / learning stays in chat.
- **Hooks = the hard-enforcement layer.** Registered in `.claude/settings.json`; scripts in `.claude/hooks/`. Unlike markdown the model *reads*, hooks *fire every time*. First one (PostToolUse formatter) shipped in g3. **Gotchas:** tool input arrives on **stdin as JSON** (`cat - | jq -r '.tool_input.file_path // empty'`) — no `CLAUDE_TOOL_INPUT_*` env var; hook command paths must be absolute via `${CLAUDE_PROJECT_DIR}/.claude/hooks/...`. g4 finding: the formatter **fires on *intermediate* edits** (it briefly stripped a momentarily-unused import; survived only because the final state used it). **Recurred in g4a exactly as predicted** (stripped `_reshape_task` on an intermediate edit) — mitigation banked: add an import **and its first use in the same edit**, or re-add it last.
- **Subagents:** own context window, custom system prompt, restricted tools, `skills:` preloads a skill in full. **Isolation verified in g3:** the verifier ran ~18 tool calls (~20k tokens of curl/log noise); none entered the orchestrator's context — only the ~200-token PASS/FAIL report did.
- **Three-layer verification model (g3):** `agents/verifier.md` = **WHO** (role, tool allowlist, output shape — stable); `verifier-web/SKILL.md` = **HOW** (launch recipe, endpoints, selectors — updated per goal); the invocation prompt = **WHAT** (this run's AC, written fresh). Never bake AC into the agent file.
- **Verification coverage (g4 lesson, corrected):** g4's `move` actually *worked* and the verifier correctly confirmed it — but manual use still surfaced a clipped menu (visual) and a non-optimistic destination (perceived latency). The honest lesson is narrow: **functional verification (API *or* UI) still misses *visual* and *latency* issues** — those need manual/visual review or explicit perf assertions. Genuine UI-flow checks earn their place for *state-machine* behaviors (undo-toast fires zero writes; deferred DELETE fires only post-window; picker reaches a no-bucket date) — `verifier-web` gains those in g4a. *(The non-optimistic destination is also a frontend-convention miss — insert-from-response, not refetch — which feeds the "did the frontend rule fire in the teammate's context?" question below.)* **g4a outcome:** the UI-flow checks were built but **BLOCKED — no browser in the verifier sandbox** → undo-zero-writes / deferred-DELETE-timing / optimistic-move / menu-clip are verified by code + endpoints **only, not by automation** (manual real-list pass recommended; getting a browser into the verifier env is the open gap, rolled to the g5 decision). The functional verifier *did* catch a real functional bug — PATCH/DELETE on an unknown id leaked **500/502** because Google returns HTTP **400** (not 404) for bad ids; `_get_task` now maps `400|404→None` and `delete` pre-checks existence (+3 regression tests).
- **Agent existence ≠ agent usage (g3):** Claude Code verified via direct curl despite the verifier existing. Enforcement: a CLAUDE.md line + the `/verify` slash command (g4) makes the right path the easy path.
- **Agent teams (g4, first run — validated):** teammates share one checkout (NOT worktree-isolated) — **file partition by layer is the safety mechanism**, and the **API contract must be frozen in the brief before spawning** (that's what made integration first-try, zero merge friction). **The lead must run every gate** — teammate sandboxes can't run project binaries (`uv`/`npm`/pytest/ruff/tsc). **Format only touched files, not whole packages** (ruff-version drift reformats untouched files → noisy diff). **Worktrees would have hurt here** — the value was a *shared* tree + a *logical* partition; isolation buys nothing when two layers share a contract (save worktrees for genuinely independent goals, g7a∥b). Lead coordination overhead is real but **front-loaded** into writing the contracts; post-spawn integration is mechanical.
- **Closing checklist** lives in `docs/goals/README.md` (read at the start of the next goal). Ran live for g2, g3, g4. Only the human guarantees it runs.

## Architecture principles
- **Read paths use the Google API directly — no MCP, no LLM.** MCP-in-Claude-Code is a dev-time tool; the backend calls Google's client libs at runtime.
- **Layering:** merge/group/order logic in `backend/app/overlay/` (owns group CRUD); `app/google/*` stays one-API-call-per-function (fetch+reshape + thin write wrappers — no orchestration; g4 added `update_due_date`/`insert_task`/`delete_task`, g4a adds task-content update + `update_tasklist`); write orchestration in `backend/app/writes/` (reschedule/move in g4; create/edit/delete in g4a); routers thin.
- **Endpoint conventions:** orchestration with multi-step side effects → **POST** named command (`/reschedule`, `/move`). Single-resource field updates → **PATCH**: `/overlay` for local fields, bare `/tasks/{list}/{task}` for Google content (title/notes). Create = `POST /tasks/{list}`; delete = `DELETE`; list rename = `PATCH /lists/{id}`. Mirrors Google's `tasks.patch` / `tasklists.patch`.
- **PATCH partial-update semantics (g3):** distinguish "field omitted" from "explicitly null" with a module-level `_UNSET` sentinel as the service default + Pydantic `model_fields_set` in the router. Applies to any nullable-on-PATCH field.
- **Prefer DB-level fallbacks to app logic** where the schema can express the rule (`ON DELETE SET NULL`). Render robustness over crashes: orphaned state degrades to standalone rendering; SQLite migrations need `render_as_batch=True`.
- The **only** runtime LLM is the auto-router (g5): structured output (entry -> destination + confidence); deterministic code does the writes; low-confidence -> review queue; grade routing with a small eval set.
- **Frontend mutation conventions:** *(local/overlay writes, g2–g3)* optimistic update, rank computed in the component, PATCH outside `setState`, never awaited, errors swallowed, one write per op, no reloads. *(Google writes, g4+)* same optimistic immediate-apply, no confirm dialogs, pre-op snapshot; failure → rollback + error toast, never swallowed. *(Destructive delete, g4a)* deferred-delete state machine: optimistic remove + undo-toast; the Google `DELETE` is held until the undo window closes; Undo cancels it.
- Skills/agents/rules emerge from real friction — don't pre-write. Same for CLAUDE.md edits. (g3: `tasks-panel.md` exists because five real DnD bugs earned it. g4a: `verifier-web` gains UI-flow checks and `writes.md` gets its first revision *because* g4 surfaced real gaps.)

## Harness learning (meta-goal)
Each goal **deliberately introduces or exercises one new Claude Code harness capability** (skill / rule / hook / subagent / command / plugin / agent view / agent team / dynamic workflow), chosen to fit that goal. Don't force-fit; err slightly toward complexity for educational value — but a goal whose honest job is feature-completion (g4a) is allowed to *exercise/refine* rather than *introduce*, and friction-driven refinement beats bolting on a capability that doesn't fit yet. Reference: https://code.claude.com/docs/en/agents (and /sub-agents, /agent-view, /agent-teams, /workflows).

**Ledger — what's used where:**

| Capability | Introduced | Notes |
| :-- | :-- | :-- |
| MCP (dev-time) | g0 | Google Workspace MCP in Claude Code; runtime uses Google client libs directly. |
| Path-scoped rules | g0 | backend + frontend rules; g2 overlay exception; g3 `buckets` rename + **`tasks-panel.md`** (DnD architecture + bug log); g4 **`writes.md`** (write safety). |
| Skill | g1 | `google-api-integration` (fetch conventions). |
| Skill | g2 | `verifier-web` (cold-start recipe; report shape). Updated each goal as the surface changes; g4a added UI-flow (Playwright) checks — **but BLOCKED, no browser in the verifier sandbox** (needs a browser in the env, or manual review; the g5 decision rolls this forward). |
| Subagent | g3 ✅ | `verifier` — preloads `verifier-web`, tools = Bash + Read, PASS/FAIL <40 lines. Isolation confirmed (~20k tokens kept out of main context). |
| Hook | g3 ✅ | PostToolUse formatter on Edit\|Write — ruff / prettier. stdin-JSON + `${CLAUDE_PROJECT_DIR}` gotchas; fires on intermediate edits (g4). |
| Slash command | g4 ✅ | `/verify` — wraps the verifier subagent with the current goal's AC; fixes the g3 verification-discipline gap. |
| Skill | g4 ✅ | `verifier-writes` — safe write verification against `zz-verifier-test` lists, with cleanup. |
| Agent team | g4 ✅ | Two teammates partitioned backend/frontend; partition held, zero merge friction; learnings banked above. |
| *(refinement)* | g4a ✅ | **No new major capability — deliberate.** Ran **inline single-session** → the 2nd agent-team run + the conclusive per-teammate rule-fire reading were **DEFERRED to g7a∥b**. `verifier-web` extended to UI-flow (Playwright) checks but **BLOCKED** (no browser) — those behaviors unverified by automation. Inline rule-fire analog: all four path-scoped rules fired in a single context. Hook gotcha recurred as predicted. The functional verifier caught a real 400-not-404 bug. |
| Dynamic workflow | g5 ✅ | `/eval` fan-out RUN LIVE, cost-instrumented (see goal-5.md wrap-up): 4× cheap-model `eval-worker` shards vs. single-loop baseline — **fan-out NOT worth it at ~32 short classifications** (~+25k orchestration tokens for ~40% wall-clock); eval gate PASS 32/32; create-only enforced as a **test/CI gate** (AST write-dependency test), not a hook. |
| *(friction-driven only)* | g6 ✅ | **Objective shift: no new capability introduced — as planned.** Upkeep delivered (`e8920dc`): `tasks-panel.md` +54 lines (shared-`DndContext` `DndListGroup` architecture), `writes.md` (`move` + optional `due_date`/`group_id`), `frontend.md` (pinned-config + resizable-grid conventions), `verifier-web` skill refresh; Playwright became a backend dev dep. |
| *(friction-driven only)* | g7 ✅ | Shipped `fbff111`. Upkeep delivered: `router.md`/`writes.md` 3rd revisions **with** the code; AST test now pins the insert-only set `{create_task, reschedule, append_note}` + the Docs/Drive call surface; eval set +6 cases (38 total, gate PASS — clear 1.0, zero task false-positives, 8/8 ambiguous below threshold); Docs/Drive conventions section earned its place in `google-api-integration`; the **owner-steps checklist pattern ran live** (`goal-7-owner-steps.md`, steps A–E executed, consent wording verified file-scoped). Rule fire/no-fire not recorded this run. |
| *(friction-driven only)* | g7a ✅ | Shipped inline single-session (no team run — the deferred parallel-worktrees rep rolls to g9). Upkeep: `insert_note`'s delimiter shape (borderBottom paragraph) documented in `writes.md`/`google-api-integration`; the AST insert-only tests pass unchanged (batchUpdate-only, no new method surface). No new capability introduced — pure daily-driver polish. Rule fire/no-fire not recorded this run. |
| *(friction-driven only)* | g7b ✅ | Calendar header strip + slimming shipped (`648d21b`, daily-driver fixes `6cbcd08` — tooltip, RSVP colors, sliver stacking, picker, prefetch). |
| *(friction-driven only)* | g7c ✅ | Daily-driver fixes (`037b90a`): create-row date picker, instant routing (inline in `POST /scratch`, scheduler → ~15-min backstop), note one-liners (documented verbatim-lock relaxation), editable review queue (task\|note only). |
| *(friction-driven only)* | g7d ✅ | Scratchpad polish (`991d729`): removed accidental-fire Shift+Enter capture (button-first now), review-queue-syncs-on-capture, "Route now" → Recent header (gated), Recent truncation + copy + tooltip, green badge for all filed states (badge-only, not whole row), dark-mode calendar-strip text fix. Frontend-only; no new capability. |
| Web OAuth + multi-tenancy | g8 ✅ | Deploy-ready (`69daa39`, PR #1): Google Sign-In web flow, row-level multi-tenancy (one `current_user` dep scoping every query), per-user Fernet-encrypted tokens + `user_settings`, Docker + Caddy + EC2 + Cloudflare DNS. First real deploy; `goal-8-owner-steps.md` (+ `-local.md`) ran live end-to-end. |
| *(friction-driven only)* | g8a ✅ | Stable notes target (`7b8b992`): under `drive.file` per-file access is keyed to the creating client id, so `ensure_notes_target` self-heals a 404'd folder/Doc (probe = `files.get`, AST insert-only surface unchanged); two durability invariants documented. Backend-only. |
| *(friction-driven only)* | g9 ✅ | Notes hierarchy (`69a90ad`): settings **tree widget** editing a per-user folder/Doc forest (materialized in Drive on save, diffed by `node_id`); hierarchical note routing (hierarchy **paths — never ids** — injected into the classifier prompt; default-Doc fallback); the one new sanctioned Drive mutation is settings-path-only `files.update(name)` rename (AST-pinned). Post-ship fixes `a809ddb`: classify/dispose split (LLM hides behind the ~5s undo toast via `POST /scratch/classify`), RECENT dedupe-by-id (kills the "Unrouted" ghost row), placeholder select-on-focus. |
| MCP (runtime-adjacent) | g10 *(was g9, g8, g6)* | Granola transcripts via MCP feeding the router pipeline. |
| ~~Agent view (parallel worktrees)~~ | ~~old g9~~ | **Dropped 2026-07-07** with the layout-prefs∥calendar goal — the 2nd agent-team / parallel-worktrees rep has no remaining natural slot. |
| ~~Plugin~~ | ~~old g10~~ | **Dropped 2026-07-07** — the `dashboard-dev` packaging capstone dies with the objective shift. |

## Goal ladder *(reordered after g3; 4a inserted post-g4; old numbering in parens)*
- 0 Harness bootstrap — **done**.
- 1 Read-only dashboard — **done** (`869040c`).
- 2 Task overlay — **done** (`dcd1307`). *(priority added here, removed in g3.)*
- 3 Task grouping & ordering — **done** (`f0f4c3f`): priority dropped; `task_group` + `group_id`; `buckets`/`items` API; group CRUD; six optimistic DnD mutations (flat SortableContext, pointerWithin-first collision); `verifier` subagent + PostToolUse hook; `tasks-panel.md` rule.
- 4 **Write-back I** *(was 6, narrowed)* — **done & verified** (`4b56cec`): reschedule via cross-bucket drag (due-date write, group-aware drop), move-to-list (insert+delete, overlay-row migration), optimistic + snapshot-rollback, no dialogs. `DndContext` lifted to one-per-list. Harness: agent team (1st run) + `/verify` command + `verifier-writes` skill + `writes.md` rule. *(Manual-use bugs: move works, but the move-to-list menu is clipped by the group boundary and the destination insert isn't optimistic → both fixed in 4a.)*
- 4a **Write-back II: full CRUD (tasks MVP)** — **done** (`8af5f3c`; committed + pushed to `main`) — create / edit (title+notes) / **complete-uncomplete** / delete tasks; **arbitrary-date picker** (reuses reschedule); **Overdue rollup**; **manual refresh**; **subtask render-robustness** (flat); rename lists; fixed g4's move-menu (portal escapes the group clip + optimistic destination via insert-from-response). Completion = immediate write + undo-toast; delete = deferred-delete + undo-toast; new tasks → `NO_DATE`; content/status edit = bare-task PATCH. **The tasks surface is now a daily-driver MVP** (API-level pass against `zz-verifier-test`; a manual real-list pass is still recommended). Harness: **ran inline single-session — 2nd agent-team run DEFERRED to g7a∥b**; `verifier-web` gained UI-flow (Playwright) checks but they were **BLOCKED** (no browser in the sandbox); the functional verifier caught a real 400-not-404 Google-error bug.
- 5 **Scratchpad + Auto-router** *(was 7+8, merged)* — **done** (`88b0844`; post-goal fixes `5d84ac1` + `9ec5f60`): capture box + local store; in-process scheduler + route-now; the **only runtime LLM** classifies (structured output → `{destination, confidence, fields}`); deterministic **create-only** writes (`{create_task, reschedule}`); review queue; eval gate **PASS 32/32**; `router.md` + `writes.md` 2nd rev + AST create-only test; `/eval` cost-instrumented fan-out run live (verdict: not worth it at this scale). Post-goal fixes: `.env` loading for `ANTHROPIC_API_KEY` (root cause of everything routing to review at 0%), exception logging in classifier + Google 502 paths, cross-panel task refresh + 45s polling, "day after" date-prompt fix.
- 6 **MVP layout: pinned lists + cross-list drag** *(objective shift starts here)* — **done** (`e8920dc`, + rename `ff01959`; includes **6a** polish): full-width `My Tasks | Follow-ups | Scratchpad` top row (scratchpad rightmost, enlarged); pinned lists static-in-code by title; drag between the pinned pair under **one shared `DndContext`** (`DndListGroup`, children-based); `move` gained optional `due_date` **and** `group_id` — cross-list + cross-bucket + into-group drop = one orchestrated write (+tests, 52 backend tests pass); **Other tasks** collapsed; calendar below the fold; no `ui_prefs`. *(6a: resizable 30/30/40 top-row grid with drag handles, ephemeral widths, stacks <1080px; pinned rows drop per-row dates to the picker icon — bucket headers carry the date, Today/Tomorrow gain weekday + `dd/mm/yyyy`; long titles ellipsize with hover; Playwright = backend dev dep, one-time `uv run playwright install chromium`.)*
- 7 **Scratchpad v2: bullet editor + notes → Google Doc** *(brief `goal-7.md`)* — **done** (`fbff111`): shipped as briefed — bullet editor (Enter continues, Tab/Shift+Tab indent, Shift+Enter captures whole-editor-as-ONE + clears), notes = 2nd live writer (verbatim top-insert, H3 IST timestamp), write set `{create_task, reschedule, append_note}` insert-only with all four guardrail artifacts revised in lockstep (eval gate PASS, 38 cases, 8/8 ambiguous below threshold); `drive.file` scoping per the ADR (bootstrap + folder-ancestry gate + startup scope assertion); owner steps A–E executed end-to-end (consent verified file-scoped; notes landing top-of-Doc in the real Doc).
- 7a **Daily-driver polish** *(brief `goal-7a.md`)* — **done**: deferred-capture undo toast (Shift+Enter stays; `POST /scratch` held ~5s, Undo = zero writes, unmount flushes); editor ≥60% of column + RECENT capped (unresolved + ~5-routed dimmed tail, own scroll); bucket color cues (header tint + 3px left edge on overdue/today/tomorrow, groups → hairline gray); top-row height caps (viewport-bound, header pinned + internal scroll); Doc-note delimiter (`borderBottom` empty-paragraph, insert-only, AST tests unchanged); rename → **"Dashboard"** across all live docs/code (`goal-7a-owner-steps.md` covers GitHub rename, remote URL, Drive-doc rename; local-folder rename left optional → settings.json paths keep the old name). tsc + build + 59 backend tests green.
- 7b **Calendar header strip + dashboard slimming** *(brief `goal-7b.md`, locked 2026-07-07; amended same day: multi-calendar + day-nav + slimming)* — **done** (`648d21b`, + daily-driver fixes `6cbcd08`: tooltip, RSVP colors, sliver stacking, picker, prefetch): `GET /calendar/day?date=` (IST day window; meet_link/attendees reshape; merges `primary` + `EXTRA_CALENDAR_IDS`, `iCalUID` dedupe, extras best-effort) + the 9a–6p proportional strip right of the title (red now-marker today-only, ±1h chevrons, prev/next-day pills + Today jump-back, hover tooltip, Meet click-through). Read-only, no scope change. Slimmed the page: below-fold `CalendarPanel` + upcoming-events endpoint removed; "Other tasks" removed; scratchpad split → 70/30.
- 7c **Daily-driver fixes** *(done, `037b90a`; brief `goal-7c.md`)* — add-task row date picker; **instant routing** (inline in `POST /scratch`, response carries routed state, scheduler → ~15-min retry backstop); note **one-liner** (timestamp → LLM summary → verbatim raw; documented relaxation of the verbatim lock, write set unchanged); review queue **editable in place**, type = task | note only.
- 7d **Scratchpad daily-driver fixes** *(done, `991d729`; brief `goal-7d.md`)* — removed the accidental-fire Shift+Enter capture (button-first now); review queue syncs on capture; "Route now" gated in the Recent header; Recent truncation + copy + tooltip; green badge for every filed state; dark-mode calendar-strip text fix. Frontend-only.
- 8 **Deploy-ready: Google Sign-In, multi-tenancy, EC2** *(done, `69daa39`, PR #1; brief `goal-8.md`)* — web OAuth = sign-in + per-user scope grants (consent published unverified); `ALLOWED_EMAILS` allowlist; signed-cookie sessions; per-user Fernet-encrypted refresh tokens + per-token scope assertion; `user_id` on every user-owned table via one `current_user` dependency; `user_settings` replaces the env vars (calendar toggle list; notes folder/Doc app-created per user); Docker + Caddy + EC2 + Cloudflare DNS; SQLite WAL + backup cron; `goal-8-owner-steps.md` (+ `-local.md`) executed end-to-end.
- 8a **Stable notes target across releases** *(done, `7b8b992`; brief `goal-8a.md`)* — under `drive.file`, per-file access is keyed to the OAuth **client id** that created the file, so a changed id 404s the notes folder/Doc forever; `ensure_notes_target` now **self-heals** (probe via `docs.file_accessible` = `files.get`; definite 404 → re-bootstrap in the same call; non-404 fails closed; cached per process). Durability invariants (persist `overlay.db`; never change the client id) documented in `deploy.md` + owner steps. Backend-only.
- 9 **Notes hierarchy: user-defined folder/Doc tree + hierarchical routing** *(done, `69a90ad`; brief `goal-9.md`)* — settings **tree widget** edits a per-user notes hierarchy (leaves = Docs, folders = inner nodes; index JSON in `user_settings`, Drive materialized eagerly on save, diffed by `node_id`); rename = the one new sanctioned Drive mutation (metadata-only `files.update(name)`, settings-path-only, AST-pinned); delete = orphan-only. The router files notes to the best-matching Doc — hierarchy **paths (never ids)** in the classifier prompt, prefix + content inference, default-Doc fallback, doc choice never gates review; body = `note_text` (raw minus routing prefix — 2nd verbatim relaxation, truncation guard, eval-graded). Doc entry shape H3 timestamp → H4 one-liner → optional H5 keywords → body → delimiter. RECENT chip shows the leaf (+ full path on hover via `routed_doc_path`). *(Post-ship `a809ddb`: classify/dispose split so the router LLM runs during the ~5s undo toast — new pure `POST /scratch/classify`, reused on commit — instead of after it; RECENT prepend dedupes by id; placeholder name inputs select-on-focus.)*
- 10 **Granola** *(was 9, was 8, was 6)* — fetch transcripts via MCP, extract action items, feed the same router/queue.
- **Dropped (2026-07-07):** old g9 layout-prefs∥calendar (visibility chooser / `ui_prefs` — revisit only if a real user hurts), old g10 plugin capstone, old g11 dependency views.

## Open design forks (lock in the brief for each goal)
- **g4a — RESOLVED (shipped `8af5f3c`):** all locks held. Subtask *nesting* stays deferred unless real day-to-day use demands it; the cross-session completed-tasks view stays deferred (g2's `show_completed` flat mode makes a "show completed" toggle a cheap later add).
- **g5 — RESOLVED (shipped `88b0844`):** all proposed locks held; `note`→Google-Doc resolved as **yes, in g7** (the Docs writer is g7's headline); create-only enforcement resolved to a **test/CI gate** (AST write-dependency test); browser-in-verifier resolved to **(b)** — eval harness was the headline verification, browser debt still open (pairs with g9 UI work).
- **g6 — RESOLVED & shipped (`e8920dc`):** cross-list + cross-bucket drop = **extend `move` with optional `due_date`** (one orchestrated backend write, writes.md rollback rules; frontend-chaining rejected) — implementation also added optional `group_id` so an into-group drop rides the same write. Pinned-by-title matching and ephemeral collapse state held. Nothing open.
- **g7 — RESOLVED (shipped `fbff111`):** all locks held (whole-editor capture, clear-on-capture, insert-at-top, `NOTES_DOC_ID` env var, verbatim plain text, plain textarea; `drive.file` Option A per the ADR). Owner steps executed; notes verified in the real Doc.
- **g7a/g7b — RESOLVED (both shipped):** all locks held (deferred-capture undo toast; RECENT tail; color cues; Doc delimiter; "Dashboard" rename; strip + slimming per the amended 7b brief).
- **g7c/g7d — RESOLVED (shipped `037b90a` / `991d729`):** synchronous routing, verbatim-lock amendment, task|note-only review types; Shift+Enter capture removed (button-first). All locks held.
- **g8/g8a — RESOLVED (shipped `69daa39` PR #1 / `7b8b992`):** publish-unverified consent, app-created per-user notes folder/Doc, EC2 + Cloudflare + Caddy single container, convention pinned titles, SQLite-stays, allowlist security floor. **First real deploy done; in multi-user daily use.** g8a's self-heal (client-id-keyed `drive.file` access) resolved as the durability fix + two documented invariants.
- **g9 — RESOLVED (shipped `69a90ad`, + post-ship fixes `a809ddb`):** user-defined notes hierarchy (tree widget, Drive materialized on save, `node_id` diff), path-not-id routing contract, orphan-only delete, settings-path-only rename (AST-pinned), 2nd verbatim relaxation with truncation guard. Post-ship: classify/dispose split hides the router LLM behind the undo toast; RECENT dedupe; select-on-focus. All locks held; nothing open.
- **Dropped forks (2026-07-07):** old-g9a `ui_prefs`/visibility-chooser and old-g9b's "+N more" expander died with the ladder cull; the shelved g9a locks remain in git history if a real user ever resurrects them.

## Status
Goals 4 (`4b56cec`), 4a (`8af5f3c`), and **5 (`88b0844`)** are **done, committed, and pushed to `main`.** g5 shipped the scratchpad capture box + local store, the in-process scheduler + route-now, the classifier (the only runtime LLM, `claude-haiku-4-5` structured output), deterministic create-only disposal (`{create_task, reschedule}`), the review queue, the eval gate (**PASS 32/32**, ambiguous 6/6 below threshold), `router.md`, the `writes.md` 2nd revision, the AST create-only test, and the live `/eval` fan-out cost experiment (verdict: fan-out not worth it at ~32 short classifications). **Post-g5 fixes** (real-use shakedown, `5d84ac1` + `9ec5f60`): `.env` loading in `main.py` (missing `ANTHROPIC_API_KEY` was silently routing everything to review at 0% — the classifier's fallback swallowed the auth error), exception logging in the classifier + Google-502 paths, cross-panel refresh (route-now/confirm → tasks panel) + 45s polling in both panels, and "day after" = TODAY+2 in the classifier prompt. The scratchpad/router is now **used-in-anger and working**.

**Objective shift (locked 2026-07-06):** the repo's primary objective is now **prod-usability for the owner's daily personal use**; harness learning is secondary (friction-driven closing-checklist upkeep only). The MVP set = **g6 + g7** shipped, then daily-driver polish (g7a–7d); the deploy goal was written and shipped as **g8** (+ g8a hardening), **g9 = notes hierarchy** followed, and Granola is now **g10** (the only laddered goal left). *(This block is historical — see the Status section for current state.)*

**g6 is done** (`e8920dc` + rename `ff01959`, pushed; includes 6a polish): the dashboard opens to the resizable `My Tasks | Follow-ups | Scratchpad` top row, cross-list drag between the pinned pair is one orchestrated write (`move` + optional `due_date`/`group_id`), Other tasks collapsed, calendar below the fold — the MVP layout is in daily use.

**g7 is done** (`fbff111`): the scratchpad is a bullet pad (Enter continues, Tab/Shift+Tab indent, Shift+Enter captures the whole editor as ONE entry + clears), and notes are a **second live writer** — verbatim top-insert under an H3 IST timestamp into the bootstrap-created Doc. Write set = `{create_task, reschedule, append_note}` (insert-only), with `router.md`/`writes.md`/AST-test/eval-gate revised in lockstep (gate PASS, 38 cases, zero task false-positives, 8/8 ambiguous below threshold). Drive access is `drive.file`-scoped per the ADR (bootstrap command, folder-ancestry gate, startup scope assertion). Owner steps A–E executed end-to-end: consent wording verified file-scoped, throwaway-then-real flip done, notes verified landing top-of-Doc — **used-in-anger and working**.

**g7a is done** (inline single-session): deferred-capture undo toast (Shift+Enter stays; `POST /scratch` held ~5s, Undo = zero writes, unmount flushes the held capture), editor ≥60% of the scratchpad column + RECENT capped (unresolved + ~5-routed dimmed tail, own scrollbar), overdue/today/tomorrow color cues (faint header tint + 3px colored left edge; group boxes demoted to hairline gray), top-row height caps (viewport-bound, header pinned + internal scroll), Doc-note `borderBottom` delimiter (insert-only, AST tests pass unchanged), and the **"Dashboard"** rename across every live doc/code string. `goal-7a-owner-steps.md` covers the non-code actions (GitHub repo rename, `git remote set-url`, Drive-doc rename; local-folder rename left optional — `.claude/settings.json` keeps the real path). tsc + frontend build + 59 backend tests green; **visual color-cue review is the one AC that needs a human eyeball** (per the g4 lesson). No new harness capability — the deferred parallel-worktrees / 2nd-team rep rolls to g9.

**g7b is done** (`648d21b`, + daily-driver fixes `6cbcd08`): the calendar strip lives in the header (day fetch merging `primary` + `EXTRA_CALENDAR_IDS`, proportional blocks, now-marker, day pills, tooltip, Meet click-through) and the page slimmed to exactly `My Tasks | Follow-ups | Scratchpad` — in daily use.

**g7c–g9 are done and shipped (2026-07-07 → 07-12).** g7c (`037b90a`) + g7d (`991d729`) closed the scratchpad daily-use fixes (create-row date picker, instant routing, note one-liners, editable review queue; Shift+Enter capture removed). **g8** (`69daa39`, PR #1) shipped deploy-ready: multi-user Google Sign-In (consent published unverified), row-level multi-tenancy behind an email allowlist, per-user encrypted tokens + `user_settings` (calendar toggles; app-created notes folder/Doc — the `EXTRA_CALENDAR_IDS`/`NOTES_*` env vars are gone), Docker + Caddy on a small EC2 with Cloudflare DNS, SQLite WAL + backups — **the app is deployed and in multi-user daily use.** **g8a** (`7b8b992`) hardened the notes target across releases (client-id-keyed `drive.file` self-heal + durability invariants). **g9** (`69a90ad`) shipped the user-defined notes hierarchy (settings tree widget → Drive folder/Doc forest) with path-not-id hierarchical routing; post-ship fixes (`a809ddb`) split classify/dispose so the router LLM runs during the ~5s undo toast, deduped RECENT, and fixed placeholder select-on-focus. Granola is now **g10**; old g9/g10/g11 (layout-prefs∥calendar, plugin capstone, dependency views) stay **dropped** — with them the visibility chooser, the parallel-worktrees/2nd-team rep, and the plugin packaging capstone.

Known DnD rough edges (tracked in `.claude/rules/tasks-panel.md`, not blocking): no DragOverlay, transform-vs-rect drift, append-only drop-on-group, approximate ungroup index, no touch support, rank precision decay. **Watch:** g6 widened the drag surface (cross-list) over no-dialog Google writes — if mis-drops become frequent in daily use, the DragOverlay fix gets promoted from rough edge to priority.

Next action: **g10 — Granola** (fetch transcripts via MCP, extract action items, feed the same router/queue) is the only remaining laddered goal; everything through g9 is deployed and in daily use. Otherwise the mode is friction-driven daily-driver fixes on the live app (e.g. the g9 post-ship latency/dedupe/UX fixes in `a809ddb`). Commit per goal; review against the brief.

<!-- Update the Status block + ladder markers + Harness ledger as goals complete. Fold durable learnings into Key decisions / Architecture principles and prune locked forks so this stays a lean live seed. -->
